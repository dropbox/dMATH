[package]
name = "simple-proofs"
version = "0.1.0"
edition = "2021"

# Exclude from parent workspace - this is a standalone test project
[workspace]

[dependencies]

[dev-dependencies]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }
