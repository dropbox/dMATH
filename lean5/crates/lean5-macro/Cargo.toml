[package]
name = "lean5-macro"
version = "0.1.0"
edition = "2021"
description = "Macro system for Lean5 - syntax quotations, macros, and syntax extensions"

[dependencies]
lean5-kernel = { path = "../lean5-kernel" }

[dev-dependencies]
