[package]
name = "tla-codegen"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "TLA2 Rust code generator from TLA+ specifications"

[dependencies]
tla-core = { path = "../tla-core" }
tla-runtime = { path = "../tla-runtime" }

[dev-dependencies]
insta = "1.34"
