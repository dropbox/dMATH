# Requirements for γ-CROWN benchmark suite
#
# VNN-COMP 2021-2024 benchmark tests
# Targets: >95% verified rate, <10s per property on ACAS-Xu
#
# Install: pip install -r requirements.txt
# Run: pytest -v --timeout=10

pytest>=7.0
pytest-xdist  # For parallel test execution
pytest-timeout  # For per-test timeouts
pytest-json-report  # For JSON test reports
